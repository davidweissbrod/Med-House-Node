import 'dotenv/config'

const DBConfig = {
    host      : "localhost",
    database  : "med-house",
    user      : "postgres",
    password  : "root",
    port      : 5432
}
export default DBConfig;